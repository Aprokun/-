#include <stdio.h>

void sort(void *arr, short elem_type, size_t size, int (*equals)(void *, void *)) {
    for (int i = 0; i < size - 1; ++i) {
        for (int j = i + 1; j < size; ++j) {
            
        }
    }
}

int name_sort()